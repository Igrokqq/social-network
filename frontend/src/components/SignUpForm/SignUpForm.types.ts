import { InputState } from '../../shared/types';

export type SignUpFormState = {
    firstName: InputState,
    lastName: InputState,
    password: InputState,
    login: InputState,
    phone: InputState,
    email: InputState,
    gender: InputState,
}
